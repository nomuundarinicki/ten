import { SearchPollsPipe } from './search-polls.pipe';

describe('SearchPollsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPollsPipe();
    expect(pipe).toBeTruthy();
  });
});
